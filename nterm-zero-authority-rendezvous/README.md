# nterm zero-authority rendezvous

This page is a harmless Browser-GPT transport diagnostic. It exposes no workstation, KTerm, shell, filesystem, subprocess, command, credential, or execution authority.

Continue only through the server-emitted link below:

- [Open the zero-authority transport canary](https://sapphiremaid.github.io/nterm-zero-authority-canary/)

The linked fixture is the existing production-shaped static canary: root → 46 emitted shard pages → 256 harmless selectors per shard. Selector targets are intentionally nonexistent. Passing this rendezvous does **not** authorize workstation control; authenticated bootstrap/authority remains a separate gate.
